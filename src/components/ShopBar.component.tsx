import { AppBar } from "@mui/material";

const ShopBar = () => {
  return (
    <AppBar position="fixed" sx={{height: "3rem", display: "flex", alignContent: "flex-start"}}>Shachar</AppBar>
  )
}

export default ShopBar;